name: auto labeler

on:
  pull_request_target:
  pull_request:
  issues:
  issue_comment:

permissions:
  contents: read
  statuses: write
  issues: write
  checks: write
  pull-requests: write

jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
      - uses: fuxingloh/multi-labeler@v4